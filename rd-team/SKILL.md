# rd-team A1 Compatibility Layer

`rd-team/` 提供 A1 兼容目录与资源入口，实际执行统一转调 `orchestrator` Python 内核。

## 约定

- `/rd-team` 的入口语义由 `rd-team/commands/rd-team.md` 描述
- 角色文档仅作为 A1 兼容资源，不复制业务逻辑
- 技术画像资源从 `config/tech-profiles.json` 同步生成
